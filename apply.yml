---
- name: Deploy {{ target }} 
  hosts: "{{ target }}"
  vars:
    ci_cd_namespace: mono-ci-cd
    test_namespace: mono-test
    prod_namespace: mono-dev
  tasks:
    - include_role:
        name: openshift-applier/roles/openshift-applier